public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=firebase
# Expo config plugin(@react-native-firebase/app等)が実行時に解決する。
# pnpmのstrict構造では見つけられずEAS Buildのconfig読み込みが失敗する。
public-hoist-pattern[]=@expo/config-plugins
